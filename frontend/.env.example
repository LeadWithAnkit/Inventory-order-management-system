# Frontend API Configuration
# For local development with Docker Compose (Vite proxy handles /api/v1):
#   VITE_API_URL=/api/v1

# For production deployment (e.g., Vercel), use your deployed backend service URL:
VITE_API_URL=https://your-backend-service-url.onrender.com/api/v1
