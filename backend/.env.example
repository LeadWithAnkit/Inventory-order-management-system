# Backend database configuration
# For Render deployment, the backend should use a single PostgreSQL connection string:
DATABASE_URL=postgresql://your_db_user:your_secure_password@your-render-postgres-host.render.com/your_database_name

# For local Docker Compose development, use the env variables defined in docker-compose.yml.
